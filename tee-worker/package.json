{
  "name": "keyring-tee-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Keyring TEE worker — AI policy judge + secret-injected HTTP execution inside a Phala dstack enclave",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "typecheck": "tsc",
    "test": "bun test"
  },
  "dependencies": {
    "@phala/dstack-sdk": "0.5.7",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.10.1",
    "typescript": "^5.9.3"
  }
}
